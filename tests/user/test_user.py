import unittest
from app.user.user import *

#  user_id, name, lastname, age, mail):

'''
class TestUser(unittest.TestCase):

    def test_createUser(self):
        user = User('223442ddd', 'Juan', 'Perez', '25', 'juancitoperez@gmail.com')
        self.assertEqual(type(user), User, 'Deberia ser un objeto de tipo User')

    def test_userName(self):
        user = User('223442ddd', 'Juan', 'Perez', '25', 'juancitoperez@gmail.com')
        self.assertEqual(user.getName(), 'Juan Perez', 'El nombre deberia ser: Juan Perez')

    def test_userAge(self):
        user = User('223442ddd', 'Juan', 'Perez', '25', 'juancitoperez@gmail.com')
        self.assertEqual(user.getAge(), '25', 'La edad deberia ser: 25')


if __name__ == '__main__':
    unittest.main()

'''